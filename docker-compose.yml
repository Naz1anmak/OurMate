services:
  bot:
    container_name: ourmate-bot
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      TZ: Europe/Moscow
    volumes:
      # Папка с birthdays.json, SQLite-базами, кэшами планировщиков и логами.
      # Bind-mount, чтобы можно было править birthdays.json и читать data/logs/
      # прямо на VPS, без захода в контейнер.
      - ./data:/app/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "5"
